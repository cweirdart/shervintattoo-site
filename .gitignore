# macOS noise
.DS_Store
**/.DS_Store
._*

# Editor/IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Local backups (use git history instead)
*.backup-*.html
*.bak

# Source files (keep these in your local archive folder, not the deployed site)
*.psd
*.ai
*.sketch
*.fig

# Build artifacts (we don't have a build step, but just in case)
node_modules/
dist/
.cache/

# Environment / secrets (we don't have any, but future-proofing)
.env
.env.local
.env.*.local

# Netlify CLI cache (if Shervin ever uses it)
.netlify/
